const mockActions = {
  setDataDetail: jest.fn(),
  add: jest.fn(),
  close: jest.fn(),
  delete: jest.fn(),
  edit: jest.fn(),
  detail: jest.fn(),
  refresh: jest.fn(),
};

export default mockActions;
